# Database connection (Neon, Supabase, or local PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/feedback_app?schema=public"

# App URL (for deployment)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
